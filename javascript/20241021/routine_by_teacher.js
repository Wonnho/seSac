// let routine
const isWeekend = true;
const nowTime = 19;

let isWorkingHour
let todo

if(nowTime>=9 && nowTime<=18) {
  isWorkingHour=true
} 
   else {
    isWorkingHour=fails;
   } 

  if(isWeekend) {
    if(isWorkingHour) {

  
      todo='relax'
    } else {
      todo='self development'
    }
  } else {
    if(isWorkingHour) {
      todo='work'
    } else {
      todo='relax'
    }
  }

