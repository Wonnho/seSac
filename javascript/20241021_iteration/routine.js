// let routine
const isWeekend = true;
const nowTime = 19;

if (isWeekend) {
  if (nowTime >= 18 && nowTime <= 24) {
    console.log('study');
  if (nowTime >= 9 && nowTime <= 18) {
    console.log('study');
  } else 
  } else {
    console.log('work');
  }
}
